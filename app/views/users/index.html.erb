<div class="container">
  <h1><%= t('.title') %></h1>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= user.title %> <%= user.first_name %> <%= user.last_name %> <%= l(user.birthdate) %>
      </li>
    <% end %>
  </ul>
</div>
b
